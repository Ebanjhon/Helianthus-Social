import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {},
  play: {opacity: 0.9},
  line: {
    position: 'absolute',
    bottom: 30,
  },
});

export default styles;
